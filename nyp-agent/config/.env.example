# nyp-agent 环境配置

# API Token（访问 Agent API 需要的密钥）
# 留空则不验证
API_TOKEN=

# API 监听地址
# 127.0.0.1 = 仅本地访问（默认，更安全）
# 0.0.0.0 = 允许远程访问（Manager 需要远程连接时使用）
API_HOST=127.0.0.1

# API 监听端口
API_PORT=3000

# 启动时是否自动开始监控 (默认: true)
# AUTO_START=true
